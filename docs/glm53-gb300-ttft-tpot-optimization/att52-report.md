# ATT52 — exact 9K recovery after ATT51 startup failure

This is a serving recovery record, **not a performance replay**. No Alex warmup or measured run was submitted. ATT50 remains the performance observation for this ten-candidate source and 9K recipe: TTFT p50/p99 2.838442/5.518384 seconds; two of 603 windows reached four seconds; valid generator; 432 uncached-load states above the engineering band. ATT47 remains the best reference satisfying both generator and load checks. No new performance pass is claimed.

ATT51's earlier returned-bootstrap candidate failed native startup on the first replacement Prefill151. All four ranks enabled the flag and completed graph capture, but bounded CPU stacks showed PP0 inside activation isend, PP1 in the newly moved bootstrap receive, and PP2/PP3 in control Work waits. This is consistent with a native progress cycle omitted by the CPU tensor-transport model. The CPU checks did not establish native correctness. The candidate was removed before any benchmark traffic.

ATT52 restored the ATT50 settings and ten-candidate source, with an inert Prefill rollout annotation refresh. The first rollback updated the DGD, PodCliqueSet and PodClique desired templates, but its replacement on P151 still carried the stale candidate. Direct pod deletion was denied by RBAC and made no change. An inert rollout annotation then refreshed both Prefills; the other six pods retained their entire identities. The controller race remains unproven. Before rollback, all 87 unaffected queue gauges were idle and Alex had zero active runs. The failed worker's idle state was not claimed. Its startup-only work was terminated through the owned deployment rollout.

After recovery, all four model-worker pods passed source, image, rank, graph and capacity checks. Eight Prefill startup logits budgets and the frozen DSA getter were verified. Fresh six canonical and four affinity checks passed. All 115 queue gauges drained twice. Both failed candidate incarnations are absent, and every tool/profiler handle has joined. Rejection sampling remains off here; it will be enabled in the separately recorded secondary 12-GPU experiment with original output policies.

Reproduction: `chart/`, `preparation/values.json`, `preparation/runtime-render.yaml` and `preparation/complete-ten-candidate-against-frozen.patch` preserve the recorded source recipe. `rollout/` retains exact preflight identities, commands and recovery observations; `rollout/refresh/` contains the final annotation rollout. `gate-execution/`, `runtime-bound.json` and `evidence/` retain the fresh checks. `replay/evidence/DRAINED.json` is explicitly recovery-only with no run ID. Failed candidate code, stacks and limits remain under `/mnt/HPC/tom/pd-latency-followup-20260913/attempt51-two-prefill-early-bootstrap`. The secondary trial must use this fresh runtime binding, because both ATT50 Prefill identities are obsolete.
