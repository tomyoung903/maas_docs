# Frontend GLM UTF-8 boundary repair

ATT54 failed at a slice inside a multibyte function name after leading whitespace. Two new CPU tests reproduce both the observed character and a no-argument Unicode case on the unchanged parser. The repair slices arguments at the original first argument-tag boundary (or end), while trimming only the extracted function name. No GPU, routing, scheduler, KV or sampling code changes.

The fixed parser passed811 unit and95 integration tests, with5 existing skips. The retained MiniJinja2.22 regression suite passed8/8. Both historical and new native libraries import in the exact retained serving image without GPU access and expose the same84 public API names and identical parser lists. This is bounded compatibility evidence, not whole-model correctness or a performance result.

The native source baseline is dc39202c7d67a76f40efab53d1cfc3485489d838, a direct child of the image-labeled ff778ed. Its MiniJinja change is retained. The committed binding lock was stale (parser5.0.0/v2.19 versus source7.0.3/v2.27). An offline resolution with many dependency downgrades was rejected. Online reconciliation preserves MiniJinja2.22 and the prior core asynchronous/HTTP dependency versions, adds required parser dependencies and resolves indexmap2.14.0→2.14.2. LOCK-RESOLUTION-REVIEW.json and both locks preserve this build limitation. No bit-for-bit reconstruction of the historical build is claimed.

Native build: existing Ubuntu builder, Rust1.93.1, standard frontend features including media/kv-indexer/slot-tracker/select-service/mm-routing/aic-forward-pass; release build4m51s. Explicit runc runtime, GPU visibility disabled,12CPU/48GiB cap. The first build stopped on the stale lock. The small renderer-test image lacked C++; the same tests then passed in the build image. All failed attempts and commands are retained. No build ran during measured traffic.

The selected artifact is sealed under runtime-native-7616ba116a7c. Wheel SHA256: 7616ba116a7c16ad9a4b6d211f6de1078e2e40faa40f4772e7936f5c84bee953; native SHA256: 462cdc2acfea49024c5aeebc25672271e633478e9137dcbc68c62a8976454864. NATIVE-ARTIFACT-VERIFIED.json lists launcher and artifact hashes. parser-fix.patch is the standalone logic/test change; retained-minijinja-baseline.patch and resolved-native-Cargo.lock reconstruct build inputs from ff778ed. Source is in source/, with exact baseline acquisition and overlay receipts. No source in Tom’s working Dynamo/SGLang checkout was modified.

ATT55 changes only the frontend hostPath wheel directory and its rollout annotation through the owned Helm/DGD recipe. Worker/image/routing/resource/storage specifications stay unchanged; all seven nonfrontend incarnations must match. The old mounted wheel directory remains the rollback target. Fresh frontend hash/API, six canonical requests, four affinity checks,400warmup and11842measured requests are required. Rust panic observer detection passed7 CPU cases including negative request/response-payload controls. Draft PRs remain deferred until the primary2P1D goal is sorted.
