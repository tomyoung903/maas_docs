# ATT51 — early bootstrap candidate rejected at native startup

**No performance replay was run.** No Alex warmup or measured requests were submitted, and no TTFT improvement is claimed. Candidate11 moves the existing incoming bootstrap-consensus receive and queue adoption before current batch selection under a uniform opt-in. It retains outgoing control order, Work commits, output handling and release consensus. The exact source is `bootstrap-candidate/scheduler_pp_mixin.py`, SHA256 `d72dff03f17bc44e856d40e9ec6d05b707cc4895455c8642c2abb534f2f9b216`; the complete eleven-candidate patch is under `preparation/`. This candidate is **not retained as a successful optimization**.

The initial Helm upgrade failed before runtime mutation because its release Secret exceeded1MiB. Excluding two disabled historical source/template pairs removed95,428unneeded packaged bytes while preserving every rendered byte and retaining all source files on disk. The retry applied successfully. Detailed failure and correction receipts remain in `rollout/failed-secret-size-01/` and `preparation/HELM-STORAGE-CORRECTION.json`.

The first replacement onP151 completed allfour graph captures and logged the opt-in active onPP0–3. It then failed to becomeReady. At02:28:44SGT, four bounded nonblocking CPU stack dumps found PP0 inside the initial activation `isend`, PP1 inside the newly moved bootstrap receive, PP2 waiting for release-send Work and PP3 waiting for bootstrap-send Work. NCCL's first pair initialization had started without a completion log. All dump helpers exited and joined; serving processes were not signaled by the profiler.

This is consistent with a native communication progress cycle. The initial CPU model validated control frames and request eligibility but let tensor-send calls return immediately and began producing fake batches immediately. The revised model delays the first batch until12idle/control iterations and blocks the host return of the first tensor send until its receiver participates. It reproduces a candidate-only failure atPP0/phase12; baseline, immediate-first-batch and preinitialized-transport controls pass. This remains a simulated mechanism check, not a native priming remedy or general correctness proof. All six cases and source hashes are retained under `evidence/native-progress-model/`.

Recovery underATT52 exposed a separate controller issue: DGD, PodCliqueSet and PodClique desired templates matched ATT50, but the replacement pod still had candidate source/flag1. Its second stack snapshot reproduced the candidate wait pattern; it was **not a failed baseline control**. UID-preconditioned directpod deletion was denied byRBAC and made no change. A permitted Prefill rollout-annotation refresh then replaced bothPrefills with the verified ATT50source; the other six pods remained unchanged. Fresh6canonical+4affinity checks and all115idle gauges qualified recovery. See `/mnt/HPC/tom/pd-latency-followup-20260913/attempt52-two-prefill-9k-recovery/REPORT.md` and the hashed recovery reference in `replay/evidence/DRAINED.json`.

Retained successful source count remains10 at9K. Original-output1P4GPU/1D8GPU with Decode rejection sampling is next. A tiny native peer-initialization test is only a research lead; no warm-transport patch has been qualified. The CPU model's earlier local-failure timing limitation is also retained: four simulatedfailure cases shared a failed timing assertion in baseline/candidate. None of these checks is a general KV correctness guarantee.
